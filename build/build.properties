##
## Environment props (must be edited by user)
##

# full path to Flex SDK
flex.sdk = C:/dev/tools/sdks/flex/4.0.0.10485

##
## Version props (edited by maintainer)
##

# Versioning
lib.version = 1.0.0
lib.name = asstream-${lib.version}

##
## Standard props (don't edit unless you have a good reason)
##

# Unit tests
test.application.name = AsStreamTestRunner.mxml
test.artifact.name = AsStreamTestRunner.swf

# Setup paths for build
build.libs.loc = ${basedir}/build/libs
src.loc = ${basedir}/src
test.src.loc = ${basedir}/test
libs.loc = ${basedir}/libs
bin.loc = ${basedir}/bin
test.bin.loc = ${basedir}/bin-test
report.loc = ${basedir}/report
report.html.loc = ${basedir}/report/html
docs.loc = ${basedir}/docs
dist.loc = ${basedir}/dist